import 'package:flutter/material.dart';

class BuildingDetailsScreen extends StatelessWidget {
  final String name;

  BuildingDetailsScreen({this.name});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text(name),
      ),
      body: Padding(
        padding: EdgeInsets.all(20),
        child: Column(
               CrossAxisAlignment.start,
      children: [
        Text('Building Name: $name'),
        SizedBox(height: 20),
        ElevatedButton(
          onPressed: () {
            // TODO: Navigate to edit building screen.
          },
          child: Text('Edit Building'),
        ),
        SizedBox(height: 20),
        ElevatedButton(
          onPressed: () {
            // TODO: Delete building from database and navigate back to home screen.
          },
          child: Text('Delete Building'),
        ),
      ],
    ),
  ),
);

